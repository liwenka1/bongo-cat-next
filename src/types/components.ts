// Logo组件Props
export interface LogoProps {
  size?: number;
  className?: string;
}

// RoundedButton组件Props
export interface RoundedButtonProps {
  onClick: () => void;
  title: string;
} 